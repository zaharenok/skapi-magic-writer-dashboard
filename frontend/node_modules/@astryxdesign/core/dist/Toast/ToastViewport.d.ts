import type { ToastPosition } from './types';
export interface ToastViewportProps {
    position?: ToastPosition;
    maxVisible?: number;
    inset?: {
        top?: number;
        bottom?: number;
        start?: number;
        end?: number;
    };
    /**
     * Promote viewport to CSS top layer via popover="manual".
     * Set to false when inside a dialog or other top-layer element.
     * @default true
     */
    isTopLayer?: boolean;
    children?: React.ReactNode;
}
/**
 * Container that renders and manages toast notifications. Place at the root
 * of your app to enable useToast(). Toasts stack with enter/exit
 * animations and auto-promote to the CSS top layer.
 *
 * @example
 * ```
 * <ToastViewport position="bottomEnd" maxVisible={3}>
 *   <App />
 * </ToastViewport>
 * ```
 */
export declare function ToastViewport({ position, maxVisible, inset, isTopLayer, children, }: ToastViewportProps): import("react").JSX.Element;
export declare namespace ToastViewport {
    var displayName: string;
}
//# sourceMappingURL=ToastViewport.d.ts.map