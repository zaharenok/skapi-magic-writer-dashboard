# SKapi Magic Writer Dashboard

Analytics + monetization dashboard for Magic Writer (trial → community subscription gate).
Built on the dashboard-almanac stack (React + Astryx + Chart.js, FastAPI, Tailscale auth).
Submodule of the main SKapi repo (path: `dashboard/`).

